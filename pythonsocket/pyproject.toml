[project]
name = "pythonsocket"
version = "0.1.0"
description = "Transferring data between programs using sockets"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.5",
]
